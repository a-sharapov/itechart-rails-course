<%provide(:current_page_title, "Add new transaction - ")%>
<section class="page-form-wrapper">
  <%= link_to "Close", user_root_path, class: "emulate-close" %>
  <h1>Create new <%= direction_transcriptor(params[:direction]).downcase %> transaction</h1>
  <%= render 'create_form', direction: params[:direction] %>
  <div class="form-links-wrapper">
    <%= link_to 'Back', user_root_path, class: "link-to-back" %>
  </div>
</section>